<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <context:annotation-config/>
    <context:component-scan base-package="com.mindflow.rpc.demo"/>

    <context:property-placeholder location="classpath:config.properties"/>

    <!-- 服务注册配置 -->
    <bean id="registryConfig" class="com.mindflow.framework.rpc.config.RegistryConfig">
        <property name="protocol" value="${registry.protocol}"/>
        <property name="address" value="${registry.address}"/>
    </bean>

    <!-- 配置 RPC 服务器 -->
    <bean id="rpcServer" class="com.mindflow.framework.rpc.server.RpcServer">
        <property name="protocol" value="" />
        <property name="registryConfig" ref="registryConfig" />
    </bean>

</beans>